import { StudentStep } from "@/components/student";

/** Step 1 — `/[locale]/student`. Gated on the disclaimer's consent checkbox. */
export default function StudentPage() {
  return <StudentStep />;
}
