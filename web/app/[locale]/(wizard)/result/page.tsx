import { ResultStep } from "@/components/result/result-step";

/** Step 3 — `/[locale]/result`. Runs `/simulate` on entry when the stored
 * result is stale and renders it. */
export default function ResultPage() {
  return <ResultStep />;
}
