/**
 * Step 1 of the wizard — identify the student.
 *
 * Only the assembled step is public: the field and the privacy note are parts
 * of this step and of no other, so they stay module-private and are reached
 * through `StudentStep`.
 */
export { StudentStep } from "./student-step";
